# Prompt: Postgres Extensions, RPC & pg_graphql

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Postgres extensions, RPC, and pg_graphql.

Review migrations and SQL. Find SECURITY DEFINER functions without pinned search_path, broad EXECUTE grants to anon/authenticated, dynamic SQL from user input, pg_graphql relationship traversal leaking cross-tenant rows, pg_net/http SSRF from the DB, pg_cron jobs with secrets/elevated roles, and pgvector retrieval not tenant-filtered.
```
