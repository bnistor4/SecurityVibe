# Prompt: Account Takeover & Identity Flows

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Account takeover and identity flows.

Trace the identity lifecycle: registration/verification, password reset (token entropy/TTL/single-use, host header poisoning), email change re-auth, OAuth state/redirect/identity linking, session rotation and logout revocation, MFA/AAL enforcement server-side, and invitation scoping.
Report exploitable chains, not just single endpoints.
```
