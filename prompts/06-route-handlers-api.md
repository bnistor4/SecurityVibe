# Prompt: Route Handlers and API

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Route Handlers and API.

Analyze app/**/route.ts, legacy API routes, and helpers. Find request.json without schema, IDOR/BOLA, weak CORS, raw errors, missing cache headers, unbounded body size, SSRF, open redirects, and unhandled HTTP methods.
```
