# Prompt: CSRF, CORS & Cross-origin

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: CSRF, CORS, and cross-origin abuse.

Find mutating Route Handlers relying only on cookies, broad allowedOrigins, reflected/wildcard CORS with credentials, endsWith/includes origin checks, GET side effects, login CSRF, postMessage without origin checks, missing SameSite/HttpOnly/Secure, and missing frame-ancestors on sensitive UI.
```
