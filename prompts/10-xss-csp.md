# Prompt: XSS, CSP and Frontend

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: XSS, CSP, Markdown/MDX, and frontend injection.

Find dangerouslySetInnerHTML, Markdown/MDX raw HTML, Script beforeInteractive with input, inline SVG, unvalidated URLs, missing/weak CSP, missing frame-ancestors, Tailwind class injection, and third-party scripts without Subresource Integrity (integrity + crossorigin).
```
