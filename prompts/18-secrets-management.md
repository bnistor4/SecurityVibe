# Prompt: Secrets & Key Management

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Secrets, keys, and token management.

Find service_role/secret keys in client-reachable code, secrets behind NEXT_PUBLIC_, committed .env/tokens, secrets in next.config env, hardcoded keys, secrets in logs/errors, provider keys used client-side, env reuse across environments, and secrets in build logs/source maps.
Recommend scanning git history, not only HEAD.
```
