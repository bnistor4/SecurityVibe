# Prompt: Supabase Edge Functions (Deno)

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Supabase Edge Functions (Deno).

Inspect supabase/functions and config.toml. Find service_role usage without per-request authz, disabled verify_jwt without compensating auth, trusted Authorization headers, unvalidated input to SQL/RPC, SSRF via Deno fetch, leaked secrets, broad CORS, missing rate limits, unsigned webhooks, and missing timeouts.
```
