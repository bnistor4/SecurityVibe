# Prompt: Version and Advisory Gate

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Version and Advisory Gate.

Check package.json, lockfile, next.config, CI/build config.
Reconcile Next.js, React, React DOM, @supabase/supabase-js, @supabase/ssr with security advisories and changelogs.
Focus on RSC RCE, Middleware/Proxy bypass, Server Actions SSRF, request smuggling, cache poisoning, Image Optimizer DoS, and Server Components DoS.
```
