# Prompt: DoS and Cost Control

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: DoS, abuse, and cost control.

Find queries without limit, select("*"), N+1, expensive counts, ReDoS regex, upload without quota, AI/email without budget, long serverless functions, image optimizer abuse, and missing rate limits.
```
