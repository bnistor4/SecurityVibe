# Prompt: Server Actions

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Server Actions.

Find actions without auth/authz, unvalidated input, mass assignment, raw record output, over-broad revalidation, input-based redirects, server fetch to user URLs, missing rate limits, and closures capturing secrets.
```
