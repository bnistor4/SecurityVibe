# Prompt: Supabase Auth SSR

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Supabase Auth SSR and session.

Find server-side getSession usage, wrong Supabase clients, cookie spoofing, refresh token handling, incomplete logout, OAuth redirect issues, magic link, OTP, MFA, and anonymous sign-in.
Verify every sensitive route/action validates the user and per-object authorization.
```
