# Prompt: Supabase Auth SSR

```text
[Use Master Prompt from master-prompt.md]
CATEGORY: Supabase Auth SSR and session.

Find server-side getSession usage, wrong Supabase clients, cookie spoofing, refresh token rotation/reuse detection gaps, JWT valid after logout, weak password policy, missing login rate limits, incomplete logout, OAuth redirect issues, magic link, OTP, MFA, signup enumeration, and anonymous sign-in.
Verify every sensitive route/action validates the user and per-object authorization.
```
